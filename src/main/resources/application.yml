server:
  port: 8899

spring:
  redis:
    host: 127.0.0.1
    port: 6379
    password:
    database: 0
    jedis:
      pool:
        max-idle: 6
        max-active: 32
        max-wait: 100
        min-idle: 4

delay-queue:
  delay-queue-name: delay_queue
  job-pool-name: delay_job_pool
  ready-queue-name: dealy_ready_queue


